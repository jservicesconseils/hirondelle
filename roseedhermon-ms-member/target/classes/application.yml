server:
  port: 8082
  ssl:
    enabled: false

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  application:
    name: ms-member
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/db_rdh}
      database: db_rdh
